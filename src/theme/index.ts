/**
 * Theme module public API.
 *
 * Import paths:
 *   - `import { baseTheme, extendTheme } from "@hca/mantine-workbench"`
 *   - or more fine-grained: `from "@hca/mantine-workbench/theme"`
 */

export { baseTheme, baseFontSizes, baseLineHeights } from "./base";
export { extendTheme } from "./extend";
